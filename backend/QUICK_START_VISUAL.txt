
╔══════════════════════════════════════════════════════════════════════════╗
║                  SKILL CONNECT PLATFORM                                  ║
║                    QUICK START GUIDE                                     ║
║                    Visual Edition                                        ║
╚══════════════════════════════════════════════════════════════════════════╝


┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 1: START INFRASTRUCTURE                                             │
│ (Open Terminal 1)                                                         │
└──────────────────────────────────────────────────────────────────────────┘

   ┌──────────────────┐
   │   PostgreSQL     │  docker run -d --name postgres -p 5432:5432 -e POSTGRES_DB=skill_connect_db -e POSTGRES_USER=admin -e POSTGRES_PASSWORD=admin123 postgres:15
   │   Database       │
   └──────────────────┘
          ↓
       ✅ Ready on port 5432


┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 2: FILE STORAGE                                                     │
│ (Open Terminal 2)                                                         │
└──────────────────────────────────────────────────────────────────────────┘

   ┌──────────────────┐
   │     MinIO        │  docker run -d --name minio -p 9000:9000 -p 9001:9001 -e MINIO_ROOT_USER=minioadmin -e MINIO_ROOT_PASSWORD=minioadmin123 minio/minio:latest server /data --console-address ":9001"
   │   Object Store   │
   └──────────────────┘
          ↓
       ✅ Ready on port 9001
       📊 Console: http://localhost:9001


┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 3: EMAIL SERVICE                                                    │
│ (Open Terminal 3)                                                         │
└──────────────────────────────────────────────────────────────────────────┘

   ┌──────────────────┐
   │    Mailpit       │  docker run -d --name mailpit -p 1025:1025 -p 8025:8025 axllent/mailpit:latest
   │  Email Testing   │
   └──────────────────┘
          ↓
       ✅ Ready on port 1025
       📧 Web UI: http://localhost:8025


┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 4: BACKEND SERVER                                                   │
│ (Open Terminal 4, cd backend)                                             │
└──────────────────────────────────────────────────────────────────────────┘

   npm install

   Then run (Windows PowerShell):
   $env:DB_HOST="localhost"; $env:DB_PORT="5432"; $env:DB_NAME="skill_connect_db"; $env:DB_USER="admin"; $env:DB_PASSWORD="admin123"; $env:MINIO_ENDPOINT="localhost"; $env:MINIO_PORT="9000"; $env:MINIO_USER="minioadmin"; $env:MINIO_PASSWORD="minioadmin123"; $env:MAILPIT_HOST="localhost"; $env:MAILPIT_PORT="1025"; $env:NODE_ENV="development"; npm start

   Or (Command Prompt):
   set DB_HOST=localhost && set DB_PORT=5432 && set DB_NAME=skill_connect_db && set DB_USER=admin && set DB_PASSWORD=admin123 && set MINIO_ENDPOINT=localhost && set MINIO_PORT=9000 && set MINIO_USER=minioadmin && set MINIO_PASSWORD=minioadmin123 && set MAILPIT_HOST=localhost && set MAILPIT_PORT=1025 && set NODE_ENV=development && npm start

   Or (Linux/Mac):
   DB_HOST=localhost DB_PORT=5432 DB_NAME=skill_connect_db DB_USER=admin DB_PASSWORD=admin123 MINIO_ENDPOINT=localhost MINIO_PORT=9000 MINIO_USER=minioadmin MINIO_PASSWORD=minioadmin123 MAILPIT_HOST=localhost MAILPIT_PORT=1025 NODE_ENV=development npm start

   ┌──────────────────┐
   │  Node Backend    │
   │  (Express.js)    │
   └──────────────────┘
          ↓
       ✅ Ready on port 5000


┌──────────────────────────────────────────────────────────────────────────┐
│ STEP 5: FRONTEND APP                                                     │
│ (Open Terminal 5, cd frontend)                                            │
└──────────────────────────────────────────────────────────────────────────┘

   npm install

   Then run (Windows):
   set REACT_APP_API_URL=http://localhost:5000 && npm start

   Or (Linux/Mac):
   REACT_APP_API_URL=http://localhost:5000 npm start

   ┌──────────────────┐
   │  React Frontend  │
   │  (Web App)       │
   └──────────────────┘
          ↓
       ✅ Ready on port 3000


╔══════════════════════════════════════════════════════════════════════════╗
║                     COMPLETE ARCHITECTURE                                ║
╚══════════════════════════════════════════════════════════════════════════╝

                    User Browser
                        │
                        ↓
            ┌─────────────────────────┐
            │  React Frontend (3000)  │
            │   - Login, Signup       │
            │   - Dashboard           │
            │   - Admin Panel         │
            └─────────────────────────┘
                        │
                        ↓
            ┌─────────────────────────┐
            │  Express API (5000)     │
            │   - Auth Routes         │
            │   - User Routes         │
            │   - Message Routes      │
            │   - Subscription Routes │
            └─────────────────────────┘
                   │         │
          ┌────────┴────┐    │
          ↓             ↓    ↓
    ┌──────────┐  ┌──────────┐  ┌──────────┐
    │PostgreSQL│  │  MinIO   │  │ Mailpit  │
    │ Database │  │ Storage  │  │  Email   │
    │ (5432)   │  │ (9000)   │  │ (1025)   │
    └──────────┘  └──────────┘  └──────────┘


╔══════════════════════════════════════════════════════════════════════════╗
║                       ACCESS POINTS                                       ║
╚══════════════════════════════════════════════════════════════════════════╝

┌─ FRONTEND ─────────────────────────────────────────────────────────────┐
│ URL: http://localhost:3000                                              │
│ Sign Up: Create new account                                             │
│ Login: Use your credentials                                             │
│ Dashboard: Manage profile, messages, resumes                            │
└─────────────────────────────────────────────────────────────────────────┘

┌─ ADMIN ────────────────────────────────────────────────────────────────┐
│ URL: http://localhost:3000/admin-login                                 │
│ Email: admin@skillconnect.com                                           │
│ Password: admin123                                                      │
│ Features: Users, Messages, Donations, Subscriptions                    │
└─────────────────────────────────────────────────────────────────────────┘

┌─ MINIO CONSOLE ────────────────────────────────────────────────────────┐
│ URL: http://localhost:9001                                              │
│ Username: minioadmin                                                    │
│ Password: minioadmin123                                                 │
│ Purpose: View uploaded files (resumes, images)                          │
└─────────────────────────────────────────────────────────────────────────┘

┌─ EMAIL TESTING ────────────────────────────────────────────────────────┐
│ URL: http://localhost:8025                                              │
│ Purpose: View all emails sent by the application                        │
│ No authentication required                                              │
└─────────────────────────────────────────────────────────────────────────┘

┌─ API BACKEND ──────────────────────────────────────────────────────────┐
│ URL: http://localhost:5000/api                                          │
│ Health Check: http://localhost:5000/health                             │
│ Purpose: RESTful API for all operations                                 │
└─────────────────────────────────────────────────────────────────────────┘


╔══════════════════════════════════════════════════════════════════════════╗
║                      TESTING SCENARIOS                                    ║
╚══════════════════════════════════════════════════════════════════════════╝

SCENARIO 1: CREATE ACCOUNT
─────────────────────────
1. Go to http://localhost:3000
2. Click "Sign Up"
3. Enter email, password, name, company
4. Click "Create Account"
5. You're logged in!

SCENARIO 2: UPLOAD RESUME
──────────────────────────
1. Go to Dashboard → Home tab
2. Click "Upload Resume"
3. Select PDF/DOC file
4. View in MinIO: http://localhost:9001

SCENARIO 3: SEND MESSAGE
─────────────────────────
1. Go to Dashboard → Messages tab
2. Click "Find People"
3. Search for another user
4. Click to start conversation
5. Type message and send
6. Message appears in real-time

SCENARIO 4: TEST SUBSCRIPTION
──────────────────────────────
1. Create 6 conversations
2. Try 6th conversation
3. Payment popup appears
4. Select plan (Monthly/Yearly)
5. Upload screenshot + enter transaction ID
6. Click "Submit Payment"
7. Go to Admin → Subscriptions
8. Click "Approve"
9. User gets email: "Subscription Approved!"

SCENARIO 5: ADMIN FUNCTIONS
────────────────────────────
1. Login as admin: admin@skillconnect.com
2. View Users with filters
3. Send bulk messages
4. View Analytics
5. Manage subscription requests
6. Approve/Reject payments


╔══════════════════════════════════════════════════════════════════════════╗
║                    TROUBLESHOOTING QUICK TIPS                            ║
╚══════════════════════════════════════════════════════════════════════════╝

❌ PostgreSQL won't start
   ✅ Check if port 5432 is in use
   ✅ Run: docker logs postgres
   ✅ Try: docker stop postgres && docker rm postgres (then restart)

❌ MinIO won't start
   ✅ Check if ports 9000/9001 are in use
   ✅ Run: docker logs minio
   ✅ Use different ports if needed

❌ Backend won't connect to database
   ✅ Verify credentials in environment variables
   ✅ Check PostgreSQL is running
   ✅ Try: docker logs postgres

❌ Frontend can't reach backend
   ✅ Verify REACT_APP_API_URL is http://localhost:5000
   ✅ Check backend is running on port 5000
   ✅ Look for errors in browser console (F12)

❌ Messages not sending
   ✅ Check backend logs
   ✅ Verify both users exist in database
   ✅ Restart backend to reinitialize tables


╔══════════════════════════════════════════════════════════════════════════╗
║                         USEFUL COMMANDS                                   ║
╚══════════════════════════════════════════════════════════════════════════╝

See running containers:
  docker ps

View container logs:
  docker logs postgres
  docker logs backend (if using Docker)

Stop all containers:
  docker stop $(docker ps -q)

Remove all stopped containers:
  docker rm $(docker ps -aq)

Connect to database:
  psql -h localhost -U admin -d skill_connect_db -W

View disk usage:
  docker system df

Clean up unused Docker data:
  docker system prune -a


╔══════════════════════════════════════════════════════════════════════════╗
║                    READY TO GO! 🚀                                       ║
╚══════════════════════════════════════════════════════════════════════════╝

All 5 services should now be running:
  ✅ PostgreSQL (Port 5432)
  ✅ MinIO (Port 9000/9001)
  ✅ Mailpit (Port 1025/8025)
  ✅ Backend (Port 5000)
  ✅ Frontend (Port 3000)

Open http://localhost:3000 in your browser and start using the platform!

Questions? Check:
- COMPLETE_SETUP.txt (Detailed setup)
- DOCKER_COMMANDS.txt (Individual commands)
- FIXES_AND_SETUP.md (Common issues)
- FINAL_SUMMARY.md (Complete overview)

═══════════════════════════════════════════════════════════════════════════
